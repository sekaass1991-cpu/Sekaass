import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taxtitan.consultancy',
  appName: 'TaxTitan Consultancy',
  webDir: 'dist',
};

export default config;
