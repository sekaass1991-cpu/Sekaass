# Add project specific ProGuard rules here.
# minifyEnabled is currently false for release builds (see app/build.gradle.kts),
# so this file has no effect yet — kept as a placeholder for when it's turned on.
